import * as tslint from "tslint";
import * as ts from "typescript";
import { SonarRuleMetaData } from "../sonarRule";
export declare class Rule extends tslint.Rules.TypedRule {
    static metadata: SonarRuleMetaData;
    private static readonly WRITE_ARRAY_PATTERNS;
    private static readonly MESSAGE;
    applyWithProgram(sourceFile: ts.SourceFile, program: ts.Program): tslint.RuleFailure[];
    private static isExported(declaration);
    private static isReadUsage(usage);
    private static isVariableWrite({expression}, usage);
    private static isElementWrite({expression}, usage);
    private static isWritingMethodCall(statement, usage);
    private static findDeclarationNode(symbol, symbols);
    private static hasCollectionType(symbol, typeChecker, symbols);
    private static isNewCollectionCreation(node);
    private static isCollectionName(str);
}
